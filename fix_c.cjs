const fs = require('fs'); const p = 'landing-page-repo/app/sfp2026/cv-builder/src/app/components/Screen3Workspace.tsx'; let c = fs.readFileSync(p, 'utf8'); c = c.split('\n').map(l => l.includes('top: rect ? rect.bottom + 12 : '"50%"",') ? l.replace('top: rect ? rect.bottom + 12 :', 'top: rect ? rect.bottom + 8 :') : l).join('\n'); c = c.split('\n').map(l => l.includes('left: rect ? \max(16px, px)\` : -356px') ? l.replace('\max(16px, px)\`', '\min(px, 60vw)\`') : l).join('\n'); fs.writeFileSync(p, c); console.log('Done 2!');
