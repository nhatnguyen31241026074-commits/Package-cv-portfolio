const fs = require('fs'); const p = 'landing-page-repo/app/sfp2026/cv-builder/src/app/components/Screen3Workspace.tsx'; let c = fs.readFileSync(p, 'utf8'); c = c.split('\n').map(l => l.includes('top: rect ? rect.top + 20 :') ? l.replace('rect.top + 20', 'rect.bottom + 12') : l).join('\n'); c = c.split('\n').map(l => l.includes('left: rect ? max(') ? '                  left: rect ? \ar(--x, \px)\ : -356px,' : l).join('\n'); fs.writeFileSync(p, c); console.log('Done!');
